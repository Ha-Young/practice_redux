const INCREASE = 'Increase';
const DECREASE = 'Decrease';
const SET_DIFF = 'SetDiff';

const increase = () => ({
  type: INCREASE
});

const decrease = () => ({
  type: DECREASE
});

const setDiff = () => ({
  type: SET_DIFF
});